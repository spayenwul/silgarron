```mermaid
graph TD
    subgraph "Одноразовая Настройка (При запуске игры)"
        A[1. Чтение `intents.json`] --> B["2. 'Измеритель Смысла' (all-MiniLM-L6-v2)"];
        B --> C[3. Каждая фраза-пример превращается в вектор (координаты смысла)];
        C --> D[4. Векторы сохраняются в 'intent_collection' ChromaDB];
    end

    subgraph "Каждый Ход Игрока"
        E[5. Команда игрока: "Размозжить череп"] --> F["6. 'Измеритель Смысла'"];
        F --> G[7. Команда превращается в вектор];
        G --> H{8. Поиск в ChromaDB};
        H -- "Найти вектор, ближайший к вектору из шага 7" --> I[9. Найден вектор примера "Ударить монстра"];
        I --> J[10. Извлекаем метаданные этого примера: `{'intent': 'COMBAT'}`];
        J --> K[11. Результат: 'COMBAT'];
    end
```